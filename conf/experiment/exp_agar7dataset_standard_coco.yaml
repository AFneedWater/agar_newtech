# @package _global_

# Example: train on a standard COCO JSON + original images directory.
# This uses a single annotation file for both train/val by default.

data:
  source: coco
  train_ann: /home/gh/agar_dataset/outputs/agar7dataset/coco/ann/countable_all_multiclass.json
  images_root: /home/gh/agar_dataset/agar7dataset
  batch_size: 8
  num_workers: 8

model:
  num_classes: 7
